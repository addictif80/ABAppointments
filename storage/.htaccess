# Uploaded ticket attachments are private customer data — never serve them
# directly, always through public/ticket-attachment.php which checks access.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order Allow,Deny
    Deny from all
</IfModule>
